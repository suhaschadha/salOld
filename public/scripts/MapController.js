'use strict';

angular.module('ClinicalTrialsUseCase')
    .controller('MapController', ['$scope', '$http', '$location', function ($scope, $http, $location) {
        var refreshDrug = function () {
            
        };
        refreshDrug();
    }]);
